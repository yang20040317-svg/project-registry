---
name: project-registry
description: 通用项目管理（双引擎：命名注册表 + 记忆层），也是 Project Guardian 体系的内核层——不是排期/甘特图，是给项目里「每一个东西」发唯一身份，并把对话与决策沉淀成可续的上下文。当项目越做越臃肿、同一模块被起了几个名字、同一个 UI 做了两遍、或「上次为什么这么定 / 上次聊到哪」没人记得时触发。覆盖：资产 ID 与语义命名规范、新建前查重闸门（防重复造轮子）、按项目类型路由资产维度（代码UI / 写作内容 / 研究调研 / 自定义）、三层记忆（会话 / 决策ADR / 项目上下文包）、开工读包与收工沉淀节奏、开环追踪、机器可读 manifest.json 导出、代码与注册表对账（未登记 / 失效登记 / 疑重）、导入级代码理解（import 依赖图 / 有效行数 / 增量扫描 / 声明与实际对账）、声明式架构校验（层间依赖规则 + 违规判定 + --strict 可接 CI）、架构演进 delta（两份 manifest + 机器收据，与文件名无关）、构思状态（开工前先登记，落盘前豁免路径校验），依赖引用完整性（写入时拦悬空依赖，check --strict-deps 清存量），前缀命名空间（未定义前缀直接拒绝，naming.md §四 是前缀权威源之一；内置维度表按项目类型取用，单一类型只认自己那张，custom/混合/未识别保留全集，跨类型借用须显式 --dims），架构闸门无事实可判（知识型项目不再得到静默的绿）。触发词：「项目管理」「登记这个模块」「给这个 UI 起个名」「项目记忆」「记住上下文」「上次聊到哪」「决策记录/ADR」「开工/收工」「项目上下文包」「资产注册表」「对账」「manifest」「Serena」「Archify」「未登记」「幽灵代码」「架构校验」「层间依赖」「依赖违规」「扫描 import」「架构演进 / delta」「开工前先登记」「构思状态」「收工体检」「构思超期」「依赖闸门」「悬空依赖」「strict-deps」「前缀闸门」「未定义前缀」「naming.md」「无事实可判」「静默绿灯」「跨类型借用」「前缀类型路由」「开工」「启动一个项目」「开始一项新工作」「启动新任务」「做任何开发任务」「项目启动了」「接手一个项目」「继续之前的项目」「开始干活前先对齐」。
version: 1.9.12
---

# project-registry ｜ 通用项目管理（命名注册表 × 记忆层）

## 它是什么，不是什么

**是**：一套让项目「不失名、不失忆」的机制。
**不是**：排期表、甘特图、任务看板、Code Review 工具、代码规范检查器。

代码只是这套机制的载体之一。一个**写作项目**的章节、一个**研究项目**的假设、一个**设计项目**的视觉件，同样需要唯一身份与可续记忆——本技能按「项目类型」路由资产维度，机制不变。

## 双引擎

```
引擎 A · 命名注册表（Registry）   →  项目里每个「东西」有唯一身份
    ID（机器键） + 语义名（人读） + 别名（俗称） + 路径 + 状态 + 依赖 + 决策
        ↕ 用同一个 ID 互相咬合
引擎 B · 记忆层（Memory）         →  项目「为什么这么定 / 上次聊到哪」可续
    L1 会话记忆 / L2 决策记忆(ADR) / L3 项目记忆(上下文包) + 开环
```

**为什么必须双引擎**：只做注册表 → 记得住"叫什么"，记不住"为什么"；只做记忆 → 记得住"聊过什么"，但同一个东西仍会被做两遍。两者靠 ID 咬合才闭环：决策卡引用资产 ID，资产卡反查决策 ID。

## 30 秒上手（不想读文档版）

一个小需求只想「随手记一笔」时，不用先走完整三步流程——一行就起项目并登记第一条资产：

```bash
registry.py quick "登录页" --type UI            # 当前目录没 .project/ 就自动建 L 档 + 登记 UI-001
registry.py quick "支付模块" --type MOD --project 我的电商 --ptype code-ui
```

`quick` = 若当前目录还没 `.project/` 就先用 **L 档（轻量）** 自动 `init`，再直接 `add` 一条资产（默认状态 `构思`，落盘后再 `set`）。项目类型默认 `custom`（任何资产前缀都合法），用 `--ptype` 覆盖；要升档改 `PROJECT.md` 的「规模」后 `init --force`。

> 它是**单行引导入口**，不是新机制：底层仍走全部命名 / 查重 / 前缀闸门，只是把「init + add」两次仪式压成一行。项目长大后再回到下面的三步工作流即可。

## 三步工作流（开工 → 过程 → 收工）

### 第 1 步 ｜ 开工：先读上下文包，再动手
读 `.project/memory/context-packet.md`（没有就 `registry.py init` 建项目）。它回答四件事：项目是什么、现在到哪、活跃资产有哪些、卡在哪。
**没读上下文包就开工 = 失忆开工。** 读完后用一句话向用户复述"我接到的状态"，确认无偏差再动。

> **「最近」的口径**：包里的活跃资产表 / 构思表 / 建议起步点 / 近期决策，一律按「最近动过」排——**日期优先，同一天按 ID 序号倒序**（NNN 单调递增永不回收，号大者必然后登记）。
> 为什么要有次级依据：「更新」列只到**月-日**（`09-25`），同一天登记的一批这个值完全相同，而 Python 排序稳定、`reverse=True` 在键相等时等于没执行。只按日期排的话，表首会站着当天**最先**登记的那个——100 资产项目里最后登记的几条一条都进不了表。
> 日期一律经 `_normalize_day()` 归一化成 `(年,月,日)` 三元组再比——**不能拿 `12-30` 这种字符串直接比**：字符串是逐字符比较，`12-30` 会压过 `01-05`，于是去年的反而排在今年前面。缺的年份按「补出来若晚于今天，那只能是去年」推断（「更新」记的是已发生的动作，没有未来日期）。残留损失：真躺了一年多的条目天数会差约一年，但方向与量级仍对，远好过一声不响。

### 第 2 步 ｜ 过程中：三道闸门
- **① 命名闸门（新建任何东西前）**：先查重 → 命中就复用/更新已有项，不另起炉灶；确需另起，先分配 ID + 语义名并登记，**再**写代码/内容。详见 `modules/02-命名注册表.md`。
  - **当场还没落盘的，状态用 `构思`**（`add --status 构思`，位置可以只写意向、也可以不写）。这是「开工前先登记」的正规写法：四个出口都对它豁免路径校验，所以它不会挡住你后面动手。
  - **写 `--deps` 时，被依赖的 ID 必须已经存在**：`add` / `set` 在**写入那一刻**就拦（退出码 2，并给同前缀候选）。依赖了还没动手的东西 → 先把它登记成 `构思`（已登记的 ID 就不拦）。
  - **`--type` 的前缀必须在本项目已定义**：未定义的前缀被拒（退出码 2，像拼错会给最近邻建议）。就地定义用 `add --type ZZ --dims "ZZ:维度名"`，或在 `registry/naming.md` §四 填一行——**§四 是真会被读的**，不是摆设。
- **② 决策闸门（非显然的决定）**：凡是「选了 A 而不是 B」「改了之前的定法」「划了不做的边界」——当场写一张决策卡（ADR）。不写 = 下次又吵一遍。详见 `modules/03-记忆层.md`。
- **③ 架构闸门（跨层依赖动过之后）**：`registry.py audit` —— 代码里的实际依赖，是否违反 `.project/architecture.json` 声明的层间规则。**没有任何依赖边时它会显式打一行「无事实可判」**（知识型项目常见），别把那个 0 读成「架构干净」。详见 `modules/05-代码理解与架构校验.md`。

### 过程中 ｜ 主动登记提醒（AI 行为，不是机器闸门）

技能被加载、且当前项目已 `init`（存在 `.project/`）时，AI 要在开发会话里主动守节奏：

- **每完成一个模块 / 结构成型一次 / 决策落定一条**，在交付说明末尾附一句登记提醒（例：「支付模块已成型，要不要现在登记成 MOD-00X？」）。**先完成本职交付，再附提醒**——提醒不阻断任务。
- **不纠缠**：同一条资产提醒过、用户跳过，就不再提；连续 3 次被跳过后本会话静默，交给收工体检兜底。
- **项目还没 `init`**：只在开工时提一次「要不要用注册表管理这个项目」，用户不接就正常干活，不追问。
- 提醒是**提议不是代写**：用户没点头就不擅自 `add`——与「只提议不裁决」同一条原则。

### 第 3 步 ｜ 收工：必沉淀
`registry.py close --summary "..."` 或手工三步：① 追加会话记录 ② 更新受影响的注册项状态 ③ 刷新上下文包 + 登记开环。
`close` 还会顺手做一次**收工体检**：把「登记了却没交代」的四类（构思未落盘 / 构思但其实已落盘 / 位置不存在 / 未登记文件）打印出来并写进会话记录的 `## 收工体检` 段，**只提示、不阻断**（退出码仍 0）。用 `--no-doctor` 跳过——跳过就等于放弃收工时唯一那一眼磁盘对账。详见 `modules/03-记忆层.md`。
开环用**竖线**分隔前三列（登记日自动填）：`--opens "事项|卡在哪|关联资产"`，例：
`registry.py close --summary "排序改到服务端" --opens "等后端接口|卡在 API-003 未联调|UI-001"`。
**收工不沉淀 = 下次从零开始。** 会话记录哪怕只有三行也要写。

## 规模分档（管理本身不许臃肿）

| 档位 | 适用 | 保留什么 |
|---|---|---|
| **L 轻量** | 资产 < 15、单人、短周期 | `PROJECT.md` + `registry/index.md` + 单文件 `memory/log.md`（会话与决策混记）+ `open-loops.md` + `context-packet.md` |
| **M 标准**（默认） | 其余 | 同上，但拆开 `memory/sessions/` 与 `memory/decisions/`，不再混记 |
| **F 完整** | 资产 > 50 或多人 / 长周期 | + `registry/entities/`（每资产一卡，`add` 时自动开卡；`check` 会校验职责/边界是否真填过）+ `registry/dependencies.md`（`graph` 生成：mermaid 图 + 循环依赖 / 上帝资产 / 悬空引用体检；带内容指纹，注册表一改就报「图已过期」）+ `registry/architecture.html`（`viz` 生成的**自包含架构图**，零依赖双击即开，给人看） |

档位写在 `PROJECT.md`，可随项目长大升档，不回头补历史。

> **档位只简化目录结构，不砍能力。** 上下文包和开环**三档全给**：L 档的会话写在 `log.md`，`context` 会切块读它；L 档的 `close --opens` 一样入 `open-loops.md`，`loop --list/--done`、`check` 的开环闸门、上下文包第 5 段全部照常工作。理由很直白——L 档是「单人短周期」，恰恰是最容易忘"下次从哪接"的场景；曾经把开环做成 M/F 专属，结果 L 档的 `--opens` 只以纯文本躺在 log.md 里，四个出口同时失明。

## 项目类型路由

| 项目是什么 | 维度定义文件（资产前缀从这来） |
|---|---|
| 代码 / UI / App / 网页 | `references/project-types/code-ui.md` |
| 写作 / 内容 / 出版物 | `references/project-types/writing-content.md` |
| 研究 / 调研 / 分析 | `references/project-types/research.md` |
| 以上都不像，或混合 | `references/project-types/custom.md`（自定义维度三问） |
| **设计类项目** | 视觉资产走 `design-workflow`，身份与记忆仍归本技能（ID 前缀 `VIS`） |

维度定义**只定义前缀与字段含义**，不定义技术实现。同一项目可混合类型（如"内容站"= code-ui + writing-content），前缀空间共享、不冲突即可。

混合类型用 `init --type "code-ui+writing-content"`（`/` `,` `&` 也认）：取两个类型维度的**并集**，同前缀去重。写错的类型名不会被静默吞掉——它会打印「未识别的类型 X」并提示用 `--dims "PFX:名称"` 补——`--dims` 是**补**不是替换：它定义的维度与类型维度**合并**（同前缀去重，同前缀时以 `--dims` 的标题为准）。另外 `--dims` 定义的维度会**同时写进 `registry/naming.md` §四**（`init --dims` 与 `add --dims` 都是）——两处同源，不再一处有、一处漏。

## 目录结构（全部落在项目根，不在技能目录）

```
<项目根>/.project/
├── PROJECT.md                  # 项目身份卡：一句话定位/类型/规模/阶段/边界
├── architecture.json           # 架构规则：层定义 + 允许/禁止的层间边（audit --init 出骨架）
├── registry/
│   ├── index.md                # 资产总表（一张表，按类型分节，人读）
│   ├── manifest.json           # 同一份数据的机器可读出口（Serena/Archify 吃这份）
│   ├── proposals.md            # reconcile 产出的对账提议（只提议，不裁决）
│   ├── dependencies.md         # [F 档] 依赖图 + 结构体检（graph 生成，带指纹）
│   ├── architecture.html       # viz 产出的自包含架构图（零依赖双击即开，违规边标红）
│   ├── imports.json / .md      # scan 产出的导入图 + 有效行数 + 声明/实际对账
│   ├── architecture-audit.md/.json   # audit 的六类判定结果
│   ├── architecture-delta.md/.json   # diff 的架构演进 + 机器收据（同内容必得同收据）
│   ├── naming.md               # 命名约定 + 自定义维度（§四 是前缀定义处，add/init 会读会写）+ 禁用词
│   ├── .idseq.json             # ID 单调游标：保证 ID 永不回收
│   ├── .scan-cache.json        # scan 的增量指纹缓存（可随时删，删了只是全量重扫）
│   └── entities/               # [F 档] 每个资产一张详卡（add 时自动开卡）
└── memory/
    ├── context-packet.md       # 开工第一读（可脚本生成）
    ├── sessions/               # [M/F] 2026-09-24-<slug>.md
    ├── decisions/              # [M/F] ADR-001-<slug>.md
    ├── open-loops.md           # 未闭环事项（三档全有，L 档也入账）
    └── log.md                  # [L 档] 单一流水
```

> **并发写是安全的**：所有写命令在 `<项目根>/.project.lock` 上取互斥锁，「读 → 改 → 写」是一个事务。两个终端同时 `add` 不会再各发一次 `UI-001`，也不会互相覆盖；等不到锁（默认 20 秒）会**直接报错**而不是偷偷写坏（曾经 6 个并发 `add` 全部 rc=0，末态只剩 1 条）。锁由操作系统持有，进程正常结束或崩溃都会自动释放——不用担心残留死锁。

## 闸门：拆成两张表，别混着读

**机器闸门**（有退出码，能被脚本 / CI 拦住）：

| 闸门 | 命令 | 触发 | 退出码 |
|---|---|---|---|
| 命名 + 查重 | `add` | 前缀未定义 / 语义名命中禁用词 / 查重命中 | 2 |
| 一致性 | `check` | 路径不存在 / ID 重复 / 决策引用悬空 / 开环无归属 / 依赖了 `废弃` | 1 |
| 架构 | `audit --strict` | 层间违规 / 循环依赖 / 出度超限 | 3 |

下面三小节是各自的细节；**表格里没写的东西就不是闸门**。

### 命名 + 查重（`add`，rc=2）

- **未注册的资产不准开工**；查重命中不准另起；**语义名命中禁用词同样拦下**——不是提示（只提示等于把改名成本推到资产落地之后）。确属例外用 `--force`，并回显放行了什么。
- **三查**：同名（字面）→ 同义（别名 / 近义）→ 同职责（做的事一样、名字不同）。三查全过才发新 ID。
- **前缀闸门**：`--type` 的前缀必须在本项目**已定义**。权威源三处取并集——`index.md` 已有分节 ∪ `naming.md` §四 自定义维度 ∪ 技能内置维度表（`UI`/`API`/`MOD`/…）。未定义就拒绝：`## ZZ` 那样的裸分节会长出来，而 `check` 照样认它，**命名空间等于没有权威**。三条出路：
  - `add --type ZZ --dims "ZZ:维度名" ...` —— 就地定义，同时写进 `naming.md` §四
  - 在 `registry/naming.md` §四 手填一行（前缀 / 维度 / 什么东西算一个）
  - 确属例外 → `--force`（会回显它会留下什么）
  > 闸门只拒绝「**从没在本项目出现过**」的前缀：已建过节的一律合法，所以老项目不会被新规则审判（与依赖闸门「不回头算存量」同一条原则）。前缀像拼错时给最近邻建议（`UII` → UI）。
  > **第三处来源按项目类型取用**：项目是**单一类型**时只认那一张内置维度表——`research` 项目登记 `UI-001` 会被拦，因为 `UI` 属于 `code-ui`，跨类型串台会稀释命名空间。`custom` / 混合类型 / 类型未标注或未识别时保留四张表的并集：`custom` 恰恰是最需要那张参考表的人，混合类型的前缀空间本就设计为共享。跨类型借用请显式 `--dims "UI:界面件"` 声明——**借用别人的维度，得先说一声**；拦截提示会写明它属于哪个类型。

### 一致性（`check`，rc=1）

`registry.py check` —— 路径存在、ID 唯一、决策引用有效、开环有归属（`loop --done` 负责闭环）、**依赖状态合法**（依赖了 `废弃` = 错，依赖了 `构思` = 警）。跑不过不许收工。
   > 「路径存在」对**构思**资产不适用：构思的位置只是意向，还没落盘。它不进 ERROR，只在末尾报一行 `PLAN 构思中 N 个未落盘`。`check` / `context` / `reconcile` / `scan` **四个出口必须一致地认识这一点**——只有其中之一认识，第一步就会撞上自己的闸门（实测四处同根因）。一旦落盘改成 `在建`，四处立刻恢复照旧报警。
   > **构思也不是「挂着不管就没事」**：挂超过 `PROJECT.md` 的「构思超期阈值」（骨架 7 天）会从 `PLAN` 升成 `WARN 构思超期未落盘`，`check --idea-days N` 可临时改（0 = 关闭）。**仍只到 WARN、不进 ERROR**——「先登记、过几天动手」是正常节奏，卡住它等于逼人不敢登记。收工时的 `close` 体检会再兜一遍。
   > 最后这条是状态机「允许被依赖」列的执行端：那张表里画了 ❌，就必须在 `check` 里真的报错，否则文档和实现对不上，闸门形同虚设。
   > **依赖引用完整性分两道门，别搞混**：写入那道在 `add` / `set --deps`（默认拦，rc=2，给同前缀候选；例外出口 `--allow-dangling-deps` 会回显放行了哪几条）；存量那道在 `check --strict-deps`（把「依赖了未登记的 ID」从 WARN **重新分级**为 ERROR，rc=1，可挂 CI）。
   > **为什么拦在写入**：`依赖` 列是 `viz` / `graph` / `audit` **三个出口共同的输入**——一条悬空边让三个读数一起偏，而收工时只表现为一行 WARN。写错的只有当时那个人、那次动作，那时改成本是零。
   > **为什么默认不严**：存量清账是逐步的。一开闸就把半年前写错的旧账变成今天的门禁，只会逼人加 `--force` 绕过——闸门就废了。所以 strict 是显式开关，不是默认值。

### 架构（`audit --strict`，rc=3）

`registry.py audit --strict` —— 层间违规 / 循环依赖 / 出度超限任一存在即**退出码 3**（不加 `--strict` 只出报告、退出 0）。规则写在 `.project/architecture.json`。
   > 报告分六类判定：`layer` / `cycle` / `fanout` / `unassigned` / `unresolved` / `ghost`。后三类**只在导入级事实源下产出**，所以换 `--source` 时报告会少三段——那是预期的。`--strict` 只看前三类，`ghost` 不会让 CI 红。
   > 匹配语义是**按顺序、第一条命中生效**：先写例外、后写总则。顺序写反会得到一张「全都允许」的规则表，却自以为在管架构——所以配完必须验一次「它真的报了至少一条」。
   > 没配 `architecture.json` 时它**只做通用体检、不报层间违规**——这是刻意的（不知道你的架构意图，不该替你判）。代价是：**不配规则就没有架构闸门**。`audit --init` 给骨架。
   > **算法同一套，阈值有两处**（别把这两件事混起来）：出度算法（目标含悬空引用、去重、排除自环）由 `_fanout_map` 单点定义，`graph` / `viz` / 本闸门完全共用——所以不会再有"同一个资产两个算法算出两个出度"。**阈值**仍分开：`graph` / `viz` 的「上帝资产」硬编码 `GOD_FANOUT = 5`，本闸门用 `architecture.json` 的 **`maxFanout`**（骨架 8，可配可关），所以一个出度 6 的资产可能一边算上帝、一边不算超限——引用读数时带上阈值。另外**索引型资产**（入口、能力清单、工具脚本读的资源表）出度天生高，判之前先问"它是目录还是什么都往这儿挂"。详见 `modules/05`。
   > **无事实可判必须说出来**：`audit` 在没有任何依赖边时必然 0 违规、rc=0 —— **看起来是通过了**。知识型 / 文档型项目这是常态（`scan` 扫不到 import → 文件 0 / 边 0）。所以此时终端与报告都会显式打一行「⚠ 本次无事实可判（文件 0 / 项目内边 0）」，并提示改用 `--source registry`。**不改退出码**（知识型没代码不是错误），但绝不做静默的绿 —— 配了规则却无事实，比「没配规则」更危险：后者有明确警告，前者看起来通过了。JSON 里同样有 `no_facts` 字段供脚本读。

### 纪律闸门（**没有机器执行者**，靠流程）

| 闸门 | 靠什么守 | 为什么机器守不了 |
|---|---|---|
| 开工读包 | AI 读 `context-packet.md`，一句话向用户复述状态，确认无偏差再动手 | 「读过」没有可观测痕迹 |
| 决策卡 | 非显然的决定当场写一张 ADR | 「非显然」是判断，不是状态 |
| 同职责查重 | 并排读两条职责句，人来判是不是一回事 | 需要语义理解 |
| 过程登记提醒 | AI 在会话里每完成一个模块附一句登记提醒（不阻断、不纠缠） | 「该不该现在登记」是节奏判断，不是状态 |

> **为什么单列这一层**：把五条并列成「硬闸门」会让人以为五道都被机器守着。实测真实分布是 **3 道机器 + 1 道半（同职责那查靠人）+ 1 道纯流程**。这不是缺陷——记忆闸门本质上不可机器化（「开工前读过上下文包」没有痕迹）——是**叙述**问题。整套技能最强的气质是「代价写在明处」（导入级 vs 符号级、两个数不相等是正常的、收据不含文件名），闸门的表述不该是这套气质里唯一的例外。

**两条不可退让的底线**（都是被实测打脸后立下的）：

- **ID 永不回收**：删行、废弃都不会让 ID 复活。**发号权威是 `.idseq.json` 游标**——它只在真正发号时前进，删行不回退；文本里出现过的号不再参与发号（曾因此被一条笔误 `UI-999` 把游标永久抬到 1000，而同一次 `check` 明明已经报了"引用了未登记的 UI-999"：脚本知道那是笔误，却拿它当"该号用过"）。游标缺失时（老项目/手删）才扫文本做一次性冷启动推断，且孤立引用不计入。游标要显式改动用 `registry.py idseq --set UI 3`（前缀与数字是**两个 token**，这是文档、`-h`、reset 提示里唯一的写法；`--set UI:3` / `UI=3` 也接受）。低于现存最大号会被拒；从被污染的高值往回调是允许的（那是修污染的唯一路径），但会打印「UI-004…UI-1000 可能被复用」的警告。
  - **唯一例外**：`init --force --reset` 会连 `.idseq.json` 一起删，游标归零后旧 ID 可能被复用。这是"ID 永不回收"唯一被打破的地方，reset 前会打印警告并留 `.project.bak-reset-<时间戳>`；要保号就用不带 `--reset` 的 `--force`，或 reset 后用 `idseq --set` 补回。
- **只提议不裁决**：`reconcile` 写 `proposals.md` 与开环，绝不直写 `index.md`。机器直写一个月就把注册表变成扫描结果的复制品。

## 与其他技能的关系（可选桥接，非强制）

- **design-workflow**：产出视觉资产时，设计走它，身份与记忆走本技能（`VIS-` 前缀）。
- **autonomous-engineering-factory / ae-factory-experience-overlay**：工程执行走它；本技能在 DISCOVER 前提供上下文包、在 RELEASE 时收决策卡。
- **starfounder-experience-alchemy**：项目结束可把本项目的决策卡与踩坑 `ingest` 进经验库。

## 接入 Project Guardian 体系（由外到内四层）

本技能默认**零依赖、单机可跑**（只靠 stdlib）。按你那张图的四层，逐层交代现在谁在干：

| 层 | 谁干 | 状态 |
|---|---|---|
| 会话入口层（固定流程） | 本技能第 1/3 步：开工读包 → 查重闸门 → 收工三步 | 已内置 |
| **代码理解层** | `registry.py scan` —— 正则解析 import，建文件级依赖图 + 有效行数 + 增量缓存 + 声明/实际对账 | **已内置（导入级）**——注意信息量取决于项目类型：代码型吃得到，知识型（技能包 / 文档 / 写作）常见「文件 1 / 边 0」，那时架构判断要走 `audit --source registry` |
| **架构校验可视化层** | `registry.py audit` 判违规（声明式层间规则）+ `diff` 出演进与机器收据 + `viz` 出图（违规边标红） | **已内置** |
| 项目元信息 & 记忆层 | 本技能内核：注册表 + 三层记忆 + `manifest.json` 唯一对外出口 | 已内置 |
| 自动化扫描脚本 | `reconcile`（文件级对账）+ `scan`（增量 / 行数 / 命名检测） | 已内置 |

**还剩两件事值得外接，都不加也能用完整机制：**

- **符号级代码理解 → Serena（MCP + LSP）**。`scan` 只到**导入级**：它能说「这层碰了那层」，
  说不出「这个函数调了那个函数 / 谁引用了这个类 / 类型层级是什么」。要这些必须接语言服务器
  （`uv tool install -p 3.13 serena-agent` → 配到 `~/.workbuddy/mcp.json`）。
  **划界铁律**：Serena 自带 memory 系统，须划清——Serena memory 只存工具操作便签（怎么 build、
  语言服务器怎么配），**项目事实与决策只写 `.project/`**，单一事实源，否则双记忆漂移。
- **多视图渲染 → Archify 等画图技能**。本技能只有「架构图」一种视图。需要时序 / 工作流 /
  数据流 / 生命周期图，或 Architecture Delta 的交互视图时，把 `export` 的 manifest 与 `diff` 的
  delta.json 交给它。注意它是**呈现层**：它校验的是**它自己生成的图**（schema / 布局 / 标签压线），
  **不扫描你的代码库**——依赖事实由 `scan` / `reconcile` 产出，再 authored 进它的 IR。

详见 `modules/04-外部接入.md`（接入协议与 manifest / delta schema，**只做指针**）与
`modules/05-代码理解与架构校验.md`（**机制的唯一权威**：规则写法、六类判定、无事实可判、三档粒度、诚实边界）。

## 模块索引

> **本技能的自我约束**：demo 一律在**临时目录**建项目验证，别让 demo 数据或临时产物混进技能目录——
> 历史上曾以自身目录当项目根跑 demo，留下过 17 资产的 `.project/` 实例（虽在 `.gitignore` 里，
> 2026-09-25 已应用户要求彻底销毁）。技能目录里不该再出现 `.project/`。

- `modules/01-项目身份.md` — 身份卡、类型选定、规模分档、边界声明
- `modules/02-命名注册表.md` — 引擎 A：ID 规则、命名三问、查重三查、状态机、别名收敛
- `modules/03-记忆层.md` — 引擎 B：三层记忆、上下文包结构、决策卡、开环
- `modules/04-外部接入.md` — **只做指针**：接入协议、manifest / delta schema、外接决策（要不要接、按什么阶段接）。`scan` / `audit` / `diff` 的机制**不在这份里**（ADR-002 已裁）
- `modules/05-代码理解与架构校验.md` — `scan` / `audit` / `diff` / `viz` 的**唯一机制权威**：规则写法、六类判定、无事实可判、三档粒度对照表、导入级 vs 符号级的诚实边界
- `references/project-types/` — 各类型的资产维度定义
- `references/templates/` — 落盘模板
- `scripts/registry.py` — `init / add / set / retire / find / list / check / context / close / loop / idseq / export / reconcile / graph / viz / scan / audit / diff`

> 子模块文件是权威源；本文件是总控与索引。

## 变更记录

- **v1.9.12**（过程中的主动登记提醒）
  - 新增「过程中 ｜ 主动登记提醒」小节：技能被加载且项目已 `init` 时，AI 每完成一个模块在交付末尾附一句登记提醒——先完成本职再提醒、不阻断、被跳过不纠缠（连续 3 次静默到收工体检兜底）、项目未 `init` 只开工时提一次。提醒是提议不是代写，用户没点头不擅自 `add`。
  - 纪律闸门表补一行「过程登记提醒」（靠流程守，机器守不了节奏判断）。
  - 自我约束注记更新：demo 实例数据（17 资产的 `.project/`）已于 2026-09-25 应用户要求彻底销毁；demo 一律在临时目录跑，技能目录不再保留实例。
- **v1.9.11**（剩余项收官）
  - `architecture.json` 的 `layers` 写成字符串（`"api":"src/api"`）或单个层条目写成字符串时，`layer_of` 不再抛 `AttributeError` 崩溃（退出码 1），改为可读报错并指出正确写法 `{"name":"层名","paths":["glob"]}`；`arch_violations` 对字符串层条目同样优雅跳过。
  - `viz` 空项目提示框越界修复：画布按「空框宽度」算（`max(W_, M*2+EMPTY_W)`），提示框 `x=48`、宽 `232`、右缘 `280` 不再超出 `328` 画布。
  - `references/templates/naming.md.tpl` §四 标题由「仅 custom 类型填」改为「任何类型都可填 · 本项目前缀的权威源之一」，与新口径（前缀闸门三源并集、任何类型缺维度都往 §四 补）对齐。
  - `render_arch_html`（原 399 行单体函数）拆为 `_arch_geometry`（构图计算）/ `_arch_edges_svg`（边计算 + SVG）/ `_arch_nodes_svg`（SVG 输出）/ `_arch_stats_html`（信息叙事）/ `_arch_html`（包装）五段 + 编排器。拆分以**六种形态 HTML 输出 sha256 逐字节一致**为验收（空 / 无依赖 / 有环 / 有幽灵 / 层间违规 / 跨列长边），输出零变化。
