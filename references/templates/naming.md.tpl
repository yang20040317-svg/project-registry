# 命名约定 · {NAME}

## 一、别名收敛表

**别名只写在 `registry/index.md` 的「别名」列，不在这里抄第二份。**
双源必然漂移，抄一次就再也同步不上。要查某个叫法对应哪个 ID → `registry.py find "<叫法>"`。

> 每次听到一个新叫法就用 `registry.py set <ID> --alias "..."` 补进去。这是"记得住名字"最便宜的投资。

## 二、本项目命名约定

- 语义名语言：（中文 / 中英双写）
- 代码标识符风格：（PascalCase / kebab-case / snake_case）
- 语义名 → 代码标识的对应规则：
- 允许的简称规则：（例：「用户持仓模块」可简称「持仓模块」，不可简称「用户模块」）

## 三、禁用词（本项目额外补充）

- （技能默认禁用：新的/新版/v2/最终版、优化/改进、临时/temp、Manager/Helper/Utils、光秃秃的「模块」「组件」）

## 四、自定义维度（任何类型都可填 · **本项目前缀的权威源之一**）

内置维度表里没有的维度填这里。填进来 = 在本项目定义了这个前缀，
`add --type <前缀>` 才会放行（`--dims "前缀:维度名"` 会直接写进这张表）。

> 前缀闸门认三个来源的并集：`index.md` 已建的分节 ∪ 这张表 ∪ 项目类型的内置维度表。
> 这张表是**自定义**那一源，不是 custom 类型专用——任何类型缺维度都往这儿补。

| 前缀 | 维度 | 什么东西算一个 | 字段提示 |
|---|---|---|---|
|  |  |  |  |
