# 怎么用 · 有什么用

> 这份文档只回答两个问题：**它到底帮我做什么**、**我具体怎么操作**。
> 所有终端回显都来自一次真实跑（`<项目根>` 是脱敏后的路径），没有手写的示例输出。
>
> 机制细节见 [`SKILL.md`](SKILL.md)，命令签名见 [`README.md`](README.md#命令速查)。

---

## 一、它有什么用

五件事，每件都对应一个**你会真的撞上**的现象。

### 1. 同一个东西被做两遍 —— 它在写入那一刻就拦住

新做一个「登录页」时，如果它已经登记过，`add` 直接拒绝（退出码 2）：

```
$ registry.py add --type UI --name 登录页
查重命中【同名 / 同位置】，按闸门不得另起 ID：
  UI-001  登录页  <>  [在建]
出路：复用它 / 与它合并 / 加 --force 并在决策卡写明差异。
```

**关键在时机**：拦在「还没写代码」的时候，而不是 code review 时。查重是三查——同名（字面）→ 同义（别名 / 近义）→ 同职责（做的事一样、名字不同）。

### 2. 「上次为什么这么定 / 上次聊到哪」—— 开工一条命令就有答案

```
$ registry.py context
已刷新 <项目根>/.project/memory/context-packet.md
```

上下文包实际内容（节选）：

```markdown
## 1 项目是什么
**电商后台** — （PROJECT.md 的一句话定位还没填）

## 2 现在到哪
- 2026-09-25-登录页骨架完成-支付模块接了接口：登录页骨架完成，支付模块接了接口

## 3 活跃资产
| ID | 语义名 | 状态 | 位置 |
|---|---|---|---|
| MOD-001 | 支付模块 | 在建 | src/mod/pay.ts |
| UI-001 | 登录页 | 在建 |  |

## 5 开环与阻塞
| 事项 | 卡在哪 | 关联资产 | 登记日 |
|---|---|---|---|
| 等后端联调 | 卡在 API-001 未就绪 | MOD-001 | 2026-09-25 |

## 6 建议起步点
1. 先解最堵的开环：等后端联调
2. 推进在建：MOD-001 支付模块、UI-001 登录页
```

注意第 6 节：它不是把资料倒给你，而是**按序排好了下一步**。这份文件是开工第一读，也是交给 AI 的上下文——所以「接上手」不靠记忆。

### 3. 登记完了、代码又变了 —— 它在收工时抓你一次

真实的一次收工（`close` 内置体检），当场抓出 2 项待交代：

```
收工体检：2 项待交代（只提示、不阻断——逐条裁决：落盘 / 登记 / 写决策卡）
  - 构思但其实已落盘：API-001 支付下单接口 → `src/api/client.ts` —— 落盘了就 `set --status 在建`
  - 未登记文件 1 个：`src/pages/login.tsx` —— 跑 `reconcile` 出完整提议
```

配套的 `reconcile` 把「磁盘 ↔ 注册表」的差异做成提议：

```
$ registry.py reconcile
对账完成：未登记 1 / 失效 1 / 命名 0 / 疑重 0 / 构思跳过 1
```

**它只提议、不裁决**——绝不直写注册表。因为机器直写一个月，注册表就变成扫描结果的复制品，人就不看了。

### 4. 架构悄悄腐坏 —— 可以变成 CI 里的一道红灯

把层间规则收窄后（`pages` 不许依赖 `api`），`audit --strict` 立刻抓到这条边并给出退出码 3：

```
$ registry.py audit --strict
  层间违规 1 / 循环 0 / 出度超限 0 / 未归类 0 / 未解析 0
→ 退出码 3
```

报告不是「一堆警告」，而是带证据的判定：

```markdown
## 1 层间违规（1）
- `src/pages/login.tsx` → `src/api/client.ts`　〔pages → api〕　rules[0] pages → api = 禁止
    - 证据：src/pages/login.tsx 里 `../api/client`
```

### 5. 「一件事做完了，却不敢开新对话」—— 收工那一刻把交接文本递给你

怕开新对话，怕的从来不是「开」，是**接不上**：不知道新话题第一句该说什么。于是同一条对话越拖越长，
拖到压缩 —— 丢细节、AI 误解、返工，你还得把背景重讲一遍。

`close` 末尾会打印一段**可直接粘贴的开场白**（真实回显见「四、第 5 步」）：

```
接着「记账小程序」继续做。项目根：<项目根>
先读 .project/memory/context-packet.md（开工第一读）再动手。
上次收工的起步点：把 store.py 的 mock 换成真实本地存储
待解开环 1 项，最堵的一条：等确认存储格式
```

**提醒的主体是这段文本，不是「去开新对话」那句话。** 另外，没跑 `close` 也没关系：
技能规程要求 AI 在一件独立的事交付完、或你说「先到这」时，主动提一句并把开场白给全。

**一句话总结这五个**：它把项目里「叫什么、为什么、卡在哪、能不能这么依赖」变成**文件**，
而不是留在某个人的脑子里或某轮对话里。

---

## 二、什么项目值得用，什么不值得

| 情况 | 建议 |
|---|---|
| 多轮对话 / 跨天推进的项目 | **值得**——不然每次开工都要重新交代 |
| 资产会变多（模块、页面、接口、章节、假设） | **值得**——命名与查重开始有收益 |
| 给 AI 接手、或与 AI 协作 | **值得**——上下文包就是给它的交接单 |
| 有分层约束、要防架构腐坏 | **值得**——`audit --strict` 能进 CI |
| 一次性脚本、跑完就删 | 不值得——`quick` 随手记一笔都算多余 |
| 纯排期管理（谁在什么时候做什么） | 不适合——这不是甘特图，去用别的工具 |

---

## 三、怎么用

### 3.1 当技能用（推荐）

把整个目录放进宿主的技能目录，例如 `~/.workbuddy/skills/project-registry/`。
之后你**不用手动调用**它——宿主按 `SKILL.md` 的 description 在下列说法下自动触发：

> 「项目启动了」「开始一项新工作」「登记这个模块」「给这个 UI 起个名」
> 「项目记忆」「记住上下文」「上次聊到哪」「决策记录 / ADR」
> 「开工 / 收工」「对账」「manifest」「架构校验」「未登记 / 幽灵代码」

### 3.2 当纯 CLI 用（不装技能）

零依赖，只用 Python 标准库，不用 `pip install` 任何东西：

```bash
git clone https://github.com/yang20040317-svg/project-registry.git
alias rg='python /path/to/project-registry/scripts/registry.py'   # 想怎么起名都行
cd <你的项目> && rg init --name "项目名" --type code-ui --scale M
```

### 3.3 最轻的入口：一行

不想先学流程，就只想「随手记一笔」：

```bash
$ registry.py quick 登录页 --type UI --status 在建
已登记 UI-001  登录页  [在建]
  下一步：若这是非显然的决定，补一张决策卡 memory/decisions/ADR-NNN-*.md
```

`quick` 是**单行引导入口**，不是新机制：当前目录没有 `.project/` 就先自动建一个 L 档（轻量）项目，
底层照走全部命名 / 查重 / 前缀闸门。项目长大后再回到三步工作流即可。

---

## 四、一个完整场景走一遍

**场景**：给「电商后台」加一个支付模块，并接一个后端下单接口。

### 第 0 步 ｜ 建项目

```bash
$ registry.py init --name 电商后台 --type code-ui --scale M
已初始化 <项目根>/.project
  类型=code-ui 规模=M
  下一步：填 PROJECT.md 的六字段，然后 registry.py add 登记资产。
```

### 第 1 步 ｜ 开工：先读上下文包，再动手

```bash
$ registry.py context      # 没有 .project/ 就先 init
```

读完后**向用户复述一句「我接到的状态」**，确认无偏差再动手。没读就开工 = 失忆开工。

### 第 2 步 ｜ 登记前先查重（闸门会拦你）

```bash
$ registry.py add --type UI --name 登录页
查重命中【同名 / 同位置】，按闸门不得另起 ID：UI-001  登录页  [在建]
出路：复用它 / 与它合并 / 加 --force 并在决策卡写明差异。
→ 退出码 2
```

被拦不是失败——**它就是这道闸门存在的意义**。确属新东西就换语义名，确需另起就 `--force` 并写清楚差异。

### 第 3 步 ｜ 还没落盘的，用「构思」先占位

```bash
$ registry.py add --type API --name 支付下单接口 --path src/api/client.ts --status 构思
已登记 API-001  支付下单接口  [构思]
  位置：src/api/client.ts
```

`构思` 的位置只是**意向**，所以路径存在性校验对它豁免——否则「先登记再动手」的第一步就会被自己的闸门卡死。
这是四个出口（`check` / `context` / `reconcile` / `scan`）一致的行为。

### 第 4 步 ｜ 过程中：前缀闸门（拼错前缀会被拦）

```bash
$ registry.py add --type ZZ --name 临时试验件
前缀闸门：ZZ 在本项目未定义。已定义：API / AST / DM / MOD / PG / ST / UI / VIS
  ...
→ 退出码 2
```

三条出路：`add --type ZZ --dims "ZZ:维度名"` 就地定义 / 在 `registry/naming.md` §四 手填一行 / `--force` 放行。
**为什么这么严**：未定义的前缀会建出 `## ZZ` 裸分节，而它照样能被 `check` 认下——命名空间就没有权威了。

### 第 5 步 ｜ 收工：必沉淀（顺带提醒你开新对话）

```bash
$ registry.py close --summary "登录页骨架完成，支付模块接了接口" \
    --opens "等后端联调|卡在 API-001 未就绪|MOD-001"
已写入 <项目根>/.project/memory/sessions/2026-09-25-登录页骨架完成-支付模块接了接口.md
已登记开环 → <项目根>/.project/memory/open-loops.md
已刷新 <项目根>/.project/memory/context-packet.md
收工体检：2 项待交代（只提示、不阻断……）
```

开环前三列用**竖线**分隔（登记日自动填）。收工不沉淀 = 下次从零开始。

`close` 末尾还会给一段**交接提示**（下面是真实回显，项目名与路径取自上面那次演示）：

```
──────────────────────────────────────────────────────────────────
✅ 这件事收工了 —— 建议开一条新对话再做下一件。
   原因：本会话的上下文已被这轮工作占住，继续聊只会越来越慢；
   压缩一旦发生，丢的是细节，代价是返工 + 你得把背景重讲一遍。

   新对话第一句直接粘这一段：

   接着「记账小程序」继续做。项目根：<项目根>
   先读 .project/memory/context-packet.md（开工第一读）再动手。
   上次收工的起步点：把 store.py 的 mock 换成真实本地存储
   待解开环 1 项，最堵的一条：等确认存储格式
──────────────────────────────────────────────────────────────────
```

为什么专门做这一段：**长会话是被恐惧喂大的** —— 怕新对话接不上，又不知道第一句说什么，于是
一条对话越拖越长，拖到压缩：丢细节 → AI 误解 → 返工 + 背景重讲一遍。所以提醒的**主体不是那句
「去开新对话」，而是这段能直接粘贴的文本** —— 粘过去，新对话开局就等价于老对话。
文本数据源就是刚刷新的上下文包，**不另建「交接卡」文件**；`--no-tip` 可关掉（脚本 / CI 里输出干净）。

### 第 6 步 ｜ 把体检抓到的补上，然后再验一遍

```bash
$ registry.py set UI-001 --path src/pages/login.tsx --status 在建
$ registry.py check
ERROR  MOD-001 位置不存在：src/mod/pay.ts
— check：1 错 / 0 警 / 3 资产
→ 退出码 1
```

（这一步的 `ERROR` 是演示里故意删掉了文件造成的——正好说明 `check` 真的在看磁盘，
不是只对着注册表自己念一遍。）

### 第 7 步 ｜ 给人看的图 / 给机器读的数据

```bash
$ registry.py viz        # 自包含架构图 HTML，双击即开；违规边标红
  资产 3 / 依赖边 1 / 循环依赖 0 / 上帝资产 0 / 悬空引用 0
$ registry.py export     # 机器可读出口
  3 资产 / 0 决策 / 1 开环
  这是机器可读出口：Serena 对账与 Archify IR 都吃这一份，不要再解析 index.md。
```

**别搞混这两个出口**：`viz` 是给人看的呈现层，`export` 的 `manifest.json` 是给机器读的事实源。

---

## 五、产出物长什么样

全部落在**项目根**的 `.project/` 下（不在技能目录）：

| 文件 | 谁读 | 作用 |
|---|---|---|
| `memory/context-packet.md` | AI + 你 | 开工第一读：项目是什么 / 现在到哪 / 活跃资产 / 卡在哪 |
| `registry/index.md` | 你 | 资产总表（一张表，按类型分节） |
| `registry/manifest.json` | 机器 | 同一份数据的机器可读出口 |
| `memory/open-loops.md` | AI + 你 | 开环台账：没闭环的事 |
| `memory/sessions/` `decisions/` | AI + 你 | 会话记录 / 决策卡 ADR |
| `registry/proposals.md` | 你 | 对账提议（只提议，不裁决） |
| `registry/architecture.html` | 你 | 自包含架构图 |

资产总表的一节实际长这样：

```markdown
## 功能模块 (MOD)

| ID | 语义名 | 别名 | 位置 | 状态 | 依赖 | 决策 | 更新 |
|---|---|---|---|---|---|---|---|
| MOD-001 | 支付模块 |  | src/mod/pay.ts | 在建 | UI-001 |  | 09-25 |
```

**依赖写 ID 不写路径**——这是双引擎咬合的地方：资产卡引用决策 ID，决策卡反查资产 ID。

## 六、接到 CI

三道机器闸门都有明确退出码，可以直接当门禁：

| 闸门 | 命令 | 退出码 |
|---|---|---|
| 命名 + 查重 | `add` | `2` |
| 一致性 | `check` | `1` |
| 架构 | `audit --strict` | `3` |

```yaml
- name: 项目一致性 + 架构闸门
  run: |
    python scripts/registry.py check --strict-deps
    python scripts/registry.py audit --strict
```

存量清账用 `check --strict-deps`（把「依赖了未登记的 ID」从 WARN 升为 ERROR）——它是**显式开关**不是默认值：
一开闸就把半年前写错的旧账变成今天的门禁，只会逼人加 `--force` 绕过。

---

## 七、常见问题

**会不会把项目搞得很重？**
按规模分档：`L` 轻量（资产 < 15、单人、短周期）只有 5 个文件，
连会话与决策都混记在一个 `log.md` 里。`M` 标准（默认）才拆开，`F` 完整才加详卡与依赖图。

**我每写一个文件都必须登记吗？**
不。闸门只在 `add` 那一刻拦（拦的是「不许重复造」）。没登记的散文件，`reconcile` 会在收工时**提议**给你，
由你裁决要不要登记。

**它和 git / Issue / 看板冲突吗？**
不冲突，管的东西不同：那些管「改动与排期」，本技能管「**身份与记忆**」——
一个东西叫什么、为什么这么定、上次聊到哪。

**它需要联网或付费能力吗？**
不需要。零依赖、纯标准库、只读写你项目内的文件。

**为什么有些产出「不报错但也什么都没查到」？**
因为它不做静默的绿。`audit` 在没有依赖边时必然 0 违规、退出码 0——**看起来像通过了**，
所以此时终端与报告都会显式打一行「本次无事实可判」，而不是让你误读那个 0。

**我不爱开新对话，怕丢上下文 —— 怎么办？**
两件事一起做，就不可怕了：① 跑 `close`（状态落盘 + 生成上下文包 + 打印可粘贴的开场白）；
② 开新对话，把那段开场白粘进去。新对话的第一句应该是
「接着 X 继续做，先读 `.project/memory/context-packet.md`」—— 上下文包那六段
（项目是什么 / 现在到哪 / 活跃资产 / 近期决策 / 开环与阻塞 / 建议起步点）就是为这一刻准备的。

**真正的风险不是「换会话」，而是「没沉淀就换会话」。** 落盘了，换不换都不丢；没落盘，
留在同一条对话里也照样会随压缩蒸发。所以这个技能的取向很明确：**宁可提醒你换会话，
也不要陪你一起把它拖长** —— 长会话不是保险，它只是把风险推迟到压缩那一刻，还顺带变慢。
